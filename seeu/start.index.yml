version: 1

seeu:
  mode: respo
  auto_discover: true

start_paths:
  - "/start8/"
  - "/6iy/"
  - "/beu/"

naming:
  id_source: foldername
  pattern: "{id}.static.yml"

visibility:
  show_all: true
  sort: alphabetical
